Running ruby password checker:

From the root of the directory run `ruby app.rb` and follow instructions

To run the specs, please install rspec using `bundle install` (again from the root directory)
then run `rspec`